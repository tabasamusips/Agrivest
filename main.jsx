import React from 'react';
import ReactDOM from 'react-dom/client';
import AgrivestComplete from './agrivestComplete';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
      <AgrivestComplete />
        </React.StrictMode>
        );
        