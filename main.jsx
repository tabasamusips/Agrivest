import React from "react";
import { createRoot } from "react-dom/client";
import UpeoComplete from "./upeoComplete.jsx";

const root = createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
      <UpeoComplete />
        </React.StrictMode>
 );