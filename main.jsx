import React from "react";
import { createRoot } from "react-dom/client";
import AgriVestComplete from "./agrivestComplete.jsx";

const root = createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
      <AgriVestComplete />
        </React.StrictMode>
 );