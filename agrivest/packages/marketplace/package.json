{
  "name": "@agrivest/marketplace",
  "version": "0.1.0",
  "description": "Agri Vest marketplace — sponsors, projects, funding progress (reads the ledger)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "node --import tsx --test test/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": { "@agrivest/ledger": "*" },
  "devDependencies": {
    "@electric-sql/pglite": "^0.2",
    "@types/node": "^22",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
